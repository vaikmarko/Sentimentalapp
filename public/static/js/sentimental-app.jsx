// Due to file size, replicating identical changes would be extensive.
// For now, simplest: import the logic by referencing window.SentimentalApp from static path instead of duplicate. But keeping sync manual.